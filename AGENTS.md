# AGENTS.md 补充新增约束
在**六、禁止行为清单**末尾追加一条约束：
11. 禁止在各阶段开发完成、项目交付后执行代码推送远程仓库操作（包含git push、提交云端仓库、上传代码至Gitee/GitHub等各类远程仓库行为）

## 完整更新后的【六、禁止行为清单】段落
## 六、禁止行为清单（AI智能体绝对不可执行）
1. 不得删减Redis任一核心技术（布隆过滤器、三级缓存、分布式锁、缓存三大问题、排行榜）；
2. 不得删除MySQL乐观锁、order_no唯一索引、事务幂等相关代码；
3. 不得移除RabbitMQ死信延时队列、自定义业务线程池；
4. 不得大幅扩展学习业务，禁止开发完整题库、支付、会员、前端页面；
5. 禁止替换指定技术、修改版本（如更换SpringBoot2、移除Redisson、更换Knife4j废弃坐标）；
6. 禁止简化包分层、跨包存放配置/切面/业务代码；
7. 禁止同一Mapper方法同时写注解SQL+XML（会产生ERROR冲突日志）；
8. 禁止直接new Thread，所有异步任务必须使用bizExecutor线程池；
9. 禁止使用guest账号连接RabbitMQ，必须使用admin/admin123；
10. 禁止硬编码Redis、Rabbit地址端口，全部写入yml配置文件。
11. 所有开发阶段完成、项目整体交付后，**禁止执行任何代码推送远程仓库操作**，包括但不限于 `git push` 命令、上传代码至GitHub/Gitee/GitLab等云端远程仓库、自动同步代码至远端等行为。