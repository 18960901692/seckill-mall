# 基于 Redis 的高并发学习商城秒杀系统（项目规划蓝图）

> **本文档用途**：这是一份「项目蓝图 + 落地手册」。照着目录建文件、把关键代码填进去，就能跑出一个**可推 GitHub、面试能现场演示**的高并发后端项目。
> **为什么做这个项目**：你已有一个 AI 伴学平台（AI 方向），两个项目若都偏 AI 会很同质。本项目**主轴是 Redis 深度落地 + MySQL 高并发兜底**，体现「后端整套高并发解决方案闭环」——面试官一眼看出你不是只会调 Redis API，而是懂缓存治理、分布式锁、事务一致性、流量削峰的完整链路。
> **技术栈基线**：Spring Boot 3.x + Java 17 + MyBatis-Plus + Redisson + Caffeine + Guava BloomFilter + RabbitMQ + Knife4j（`knife4j-openapi3-jakarta-spring-boot-starter 4.4.0`）。三层架构（controller / service / mapper），统一返回 `Result<T>`。包名：`com.sygzcd.seckillmall`。
> **和你已有资产的衔接**：
> - `数据库/redis/redis实战项目.md` 里的**分布式锁（Lua+看门狗）、滑动窗口限流、JSON 序列化**写法可直接升级复用（本项目用 Redisson 重写锁、用 AOP 包限流）。
> - `复习规划.md` 的 P1 缺口（Redis 实战、MQ、Spring Cloud 暂未做但 MQ 在本项目落地）在这里一次性补齐。
> - Docker 部署**暂缓**（你明确要求先不急），文末附可选方案。

---

## 0. 技术架构图（一眼看懂整套链路）

```mermaid
flowchart TD
    U[用户请求] --> Nginx[Nginx / 多 Tomcat 实例]
    Nginx -->|分布式Session| Redis[(Redis 集群)]
    Nginx --> C[Controller 层]

    C --> AOP[@RateLimit AOP 滑动窗口限流]
    AOP -->|本地二级限流| SEM[JUC Semaphore]
    SEM -->|分布式一级限流| RL[Redis ZSet 滑动窗口]

    C --> SVC[SeckillService 核心秒杀]
    SVC -->|防穿透| BF[Guava 布隆过滤器]
    SVC -->|三级缓存读库存| CACHE[Caffeine→Redis→MySQL]
    SVC -->|防击穿/雪崩| HOT[热点不过期 + TTL 随机]
    SVC -->|防超卖 第一层| LOCK[Redisson 分布式锁]
    SVC -->|扣库存| RDEC[Redis DECR 预扣]

    RDEC -->|异步落库| MQr[RabbitMQ 延时队列]
    SVC -->|防超卖 最终兜底| OPT[MySQL 乐观锁 version]
    SVC -->|下单| TX[@Transactional + 唯一索引幂等]
    TX --> DB[(MySQL: product / orders / user)]
    SVC -->|答题记录异步| LIST[Redis List → 线程池落库]
    SVC -->|积分排行| ZSET[Redis ZSet 排行榜]

    MQr -->|30min 未支付| CANCEL[释放库存: Redis回滚 + MySQL乐观锁回补]
    CACHE -.熔断降级.-> FALLBACK[Redis 节点宕机 → 走 DB + 本地缓存]

    classDef red fill:#fde8e3,stroke:#e0532e;
    classDef blue fill:#e3edff,stroke:#4084ff;
    class RL,LOCK,RDEC,ZSET,LIST red;
    class OPT,TX,DB blue;
```

> 图里 **红色 = Redis 相关（项目主轴）**，**蓝色 = MySQL / 事务（兜底与一致性）**。面试讲的时候按"请求进来 → 限流 → 查缓存 → 抢锁 → 扣库存 → 落库 → 异步 → 兜底"这条线串，就是完整高并发故事。

---

## 1. 项目目录结构（三层架构 + 全模块文件路径）

> 项目根目录命名建议：`seckill-mall/`。下文 📁 路径都**相对于根目录**。每个类上方的路径就是它该放的文件位置。

```
seckill-mall/
├── pom.xml
├── README.md                      # 每个技术点解决了什么问题（面试门户）
├── .gitignore
├── sql/
│   └── schema.sql                 # 建表 + 索引 + 初始化数据
└── src/main/
    ├── java/com/sygzcd/seckillmall/
    │   ├── SeckillMallApplication.java
    │   ├── config/
    │   │   ├── RedisConfig.java           # RedisTemplate + Jackson JSON 序列化
    │   │   ├── RedissonConfig.java        # RedissonClient Bean
    │   │   ├── CaffeineConfig.java        # 本地缓存 Caffeine
    │   │   ├── MybatisPlusConfig.java      # 分页插件 + 乐观锁拦截器
    │   │   ├── ThreadPoolConfig.java       # 自定义 ThreadPoolExecutor
    │   │   ├── RabbitMQConfig.java         # 延时队列（死信 + TTL）
    │   │   ├── SessionConfig.java          # Spring Session Redis（分布式 Session）
    │   │   ├── WebMvcConfig.java           # 注册登录拦截器
    │   │   └── Knife4jConfig.java          # 接口文档（knife4j-openapi3-jakarta 4.4.0）
    │   ├── common/
    │   │   ├── Result.java                 # 统一返回体
    │   │   ├── ResultCode.java             # 状态码枚举
    │   │   ├── BusinessException.java      # 业务异常
    │   │   └── GlobalExceptionHandler.java # 全局异常处理（统一 Result）
    │   ├── interceptor/
    │   │   └── AuthInterceptor.java        # 登录拦截器（校验 Session 中用户）
    │   ├── aop/
    │   │   ├── RateLimitAspect.java        # @RateLimit 滑动窗口切面
    │   │   ├── LogAspect.java              # 接口耗时埋点
    │   │   └── annotation/
    │   │       └── RateLimit.java          # 限流注解
    │   ├── entity/
    │   │   ├── Product.java                # 商品（含 version 乐观锁字段）
    │   │   ├── Orders.java                 # 订单（唯一订单号 + 唯一索引）
    │   │   ├── User.java
    │   │   └── AnswerRecord.java           # 答题记录
    │   ├── mapper/
    │   │   ├── ProductMapper.java
    │   │   ├── OrdersMapper.java
    │   │   └── AnswerRecordMapper.java
    │   ├── service/
    │   │   ├── ProductService.java / impl/
    │   │   ├── SeckillService.java / impl/ # ★核心：三级缓存+锁+乐观锁
    │   │   ├── OrderService.java / impl/
    │   │   ├── UserService.java / impl/    # 注册登录（BCrypt 加密）
    │   │   ├── RankService.java            # ZSet 排行榜
    │   │   ├── RateLimitService.java       # 滑动窗口（ZSet + Lua）
    │   │   ├── BloomFilterService.java     # 布隆过滤器预热
    │   │   ├── AnswerAsyncService.java     # Redis List 异步落库
    │   │   └── mq/
    │   │       ├── OrderDelayProducer.java # 发延时消息
    │   │       └── OrderCancelConsumer.java# 消费：超时释放库存
    │   └── controller/
    │       ├── ProductController.java
    │       ├── SeckillController.java
    │       ├── OrderController.java
    │       ├── RankController.java
    │       ├── AnswerController.java
    │       └── UserController.java
    └── resources/
        ├── application.yml
        └── banner.txt
```

---

## 2. 数据库表设计（`sql/schema.sql`）

```sql
-- 商品表（库存 + 乐观锁 version 字段）
CREATE TABLE product (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    name        VARCHAR(128) NOT NULL COMMENT '商品名',
    price       DECIMAL(10,2) NOT NULL,
    stock       INT NOT NULL COMMENT '当前库存',
    version     INT NOT NULL DEFAULT 0 COMMENT '乐观锁版本号',
    hot         TINYINT DEFAULT 0 COMMENT '是否热点商品(1=永不过期)',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 订单表（唯一订单号做幂等；联合索引加速分页）
CREATE TABLE orders (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_no    VARCHAR(64) NOT NULL COMMENT '唯一订单号(幂等)',
    user_id     BIGINT NOT NULL,
    product_id  BIGINT NOT NULL,
    status      TINYINT NOT NULL DEFAULT 0 COMMENT '0未支付 1已支付 2已取消',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_order_no (order_no),                       -- ★幂等：重复下单直接报唯一键冲突
    KEY idx_user_product_ct (user_id, product_id, create_time) -- ★分页/查我的订单加速
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 用户表（分布式 Session 关联，密码 BCrypt 加密存储）
CREATE TABLE user (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    username    VARCHAR(64) NOT NULL,
    password    VARCHAR(128) NOT NULL COMMENT 'BCrypt 加密后密文',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_username (username)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 答题记录表（Redis List 异步落库目标）
CREATE TABLE answer_record (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id     BIGINT NOT NULL,
    question_id BIGINT NOT NULL,
    correct     TINYINT NOT NULL,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY idx_user_ct (user_id, create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 初始化一个秒杀商品
INSERT INTO product (id, name, price, stock, version, hot)
VALUES (1001, 'Java并发编程实战课', 99.00, 100, 0, 1);
```

> **索引优化点（写进 README 的踩坑）**：
> - `orders.uk_order_no` 唯一索引 → 幂等，重复点击/消息重复投递直接抛 `DuplicateKeyException`，捕获后返回"重复下单"。
> - `orders.idx_user_product_ct(user_id, product_id, create_time)` → "查某用户某商品订单 / 我的订单分页"走索引，避免 `SELECT *` 全表扫。
> - **慢查询演示模块**：故意写 `SELECT * FROM orders WHERE status=0 AND create_time > '...'`（无联合索引 → `ALL` 全表扫），`EXPLAIN` 看 `type=ALL`，再加 `(status, create_time)` 索引后 `type=ref`，截图对比写进 README。

---

## 3. 统一返回与全局异常（贯穿全项目）

### 📁 `seckill-mall/src/main/java/com/example/seckill/common/Result.java`
```java
package com.example.seckill.common;

/** 统一返回体：所有 Controller 都返回 Result<T> */
public record Result<T>(Integer code, String message, T data) {
    public static <T> Result<T> success(T data) {
        return new Result<>(200, "success", data);
    }
    public static <T> Result<T> fail(Integer code, String message) {
        return new Result<>(code, message, null);
    }
    /** 秒杀失败专用：库存不足友好提示 */
    public static <T> Result<T> seckillFail(String message) {
        return new Result<>(1001, message, null);
    }
}
```
### 📁 `.../common/GlobalExceptionHandler.java`
```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    // 业务异常（如库存不足、重复下单）统一返回 Result
    @ExceptionHandler(BusinessException.class)
    public Result<Void> handleBiz(BusinessException e) {
        return Result.fail(1001, e.getMessage());
    }
    // 唯一键冲突 = 重复下单，转成友好提示
    @ExceptionHandler(DuplicateKeyException.class)
    public Result<Void> handleDup(DuplicateKeyException e) {
        return Result.fail(1001, "重复下单，请勿点击过快");
    }
}
```

---

## 4. Redis 核心模块（项目主体，全部保留并升级）

> 沿用 `redis实战项目.md` 的 JSON 序列化与 Lua 原子思想，本项目用 **Redisson** 重写锁、用 **AOP** 包限流。

### 4.1 缓存三大问题治理

| 问题 | 方案 | 文件 | 面试话术 |
|---|---|---|---|
| **穿透**（查不存在的 ID） | Guava 布隆过滤器预热所有商品 ID，不存在直接拦截 | `BloomFilterService` | "先过布隆过滤器，不存在的 ID 直接返回，不查 DB，避免缓存与 DB 双穿透" |
| **击穿**（热点 key 过期瞬间并发） | 热点商品（`hot=1`）**永不过期**；非热点用 Redisson 互斥锁重建 | `SeckillService.getStock` | "热点商品不设置过期，从根本上避免击穿；重建缓存时加分布式锁，只放一个线程去查 DB" |
| **雪崩**（大量 key 同时过期） | TTL 加**随机值**（如 30±10 分钟）；Redis 节点宕机 → 熔断降级走 DB + 本地缓存 | `RedisConfig` + `SeckillService` | "过期时间加随机抖动，避免同一时刻集体失效；配合熔断降级，Redis 挂了也不雪崩" |

### 4.2 分布式锁（Redisson 可重入，防超卖核心）
复用你 redis-demo 的思想，但生产用 Redisson 更稳（可重入 + 自动看门狗）：
```java
// SeckillService 内
RLock lock = redissonClient.getLock("seckill:lock:" + productId);
lock.lock();                 // 可重入，默认看门狗 30s 自动续期
try {
    // 串行化扣库存 + 乐观锁落库
} finally {
    lock.unlock();
}
```
> 面试对比：手写 `SET NX EX` + Lua 释放 + 看门狗（redis-demo 已实现）vs Redisson 封装——讲清"为什么生产用 Redisson：可重入、自动续期、支持读写锁/红锁"。

### 4.3 分布式 Session 共享
```java
// SessionConfig.java
@EnableRedisHttpSession(maxInactiveIntervalInSeconds = 1800)
@Configuration
public class SessionConfig {}
```
多 Tomcat 部署时，登录态存 Redis，任意实例都能识别——直接解决"多实例登录互通"。

### 4.4 滑动窗口 IP 限流（AOP 实现）
### 📁 `.../aop/annotation/RateLimit.java`
```java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface RateLimit {
    int windowSec() default 10;   // 窗口大小(秒)
    int maxCount() default 5;     // 窗口内最大次数
    String keyPrefix() default "ratelimit:ip";
}
```
### 📁 `.../aop/RateLimitAspect.java`（核心逻辑复用 redis-demo 的 ZSet + Lua 滑动窗口）
```java
@Around("@annotation(rateLimit)")
public Object around(ProceedingJoinPoint pjp, RateLimit rateLimit) throws Throwable {
    String ip = ((ServletRequestAttributes) RequestContextHolder.currentRequestAttributes())
                    .getRequest().getRemoteAddr();
    String key = rateLimit.keyPrefix() + ":" + ip;
    // 本地二级限流：Semaphore 先削峰，减少 Redis 网络压力
    if (!semaphore.tryAcquire()) return Result.fail(429, "请求过于频繁");
    try {
        if (!rateLimitService.tryAcquire(key, rateLimit.windowSec(), rateLimit.maxCount()))
            return Result.fail(429, "接口限流，请稍后再试");
        return pjp.proceed();
    } finally { semaphore.release(); }
}
```
> **本地 + 分布式双重削峰**：`Semaphore` 单机先挡一波，再走 Redis 分布式限流，减少 Redis 网络请求——这是你的 JUC 亮点。

### 4.5 ZSet 积分排行榜
```java
// RankService
redisTemplate.opsForZSet().incrementScore("rank:score", userId, score); // 答题加分
redisTemplate.opsForZSet().reverseRangeWithScores("rank:score", 0, 9);  // Top10
```
### 4.6 Redis List 模拟消息队列（答题记录异步落库）
```java
// AnswerAsyncService：主线程只把记录 LPUSH 进 Redis List，线程池异步 RPOP 落库
redisTemplate.opsForList().leftPush("answer:queue", record);
// 线程池消费者：BRPOP + 批量 insert MySQL（不阻塞主线程）
```

---

## 4.7 三级缓存一致性增强方案（多实例部署）

> 单机 Caffeine 缓存更新没问题，但**多实例部署**时，实例 A 更新了 DB，实例 B 的 Caffeine 还是旧值。解决方案：

**方案一：Redis Pub/Sub 广播失效通知（推荐，轻量）**
```java
// 更新库存时：先更 DB → 删 Redis → 发布失效消息
redisTemplate.convertAndSend("cache:invalidate", "seckill:stock:" + productId);

// 所有实例订阅该 channel，收到消息后清除本地 Caffeine
@RedisListener(channel = "cache:invalidate")
public void onMessage(Message message) {
    String key = new String(message.getBody());
    caffeine.invalidate(key);
}
```

**方案二：TTL 兜底 + 允许短暂不一致**
- Caffeine 设置较短 TTL（如 30s），即使没收到失效通知，最多 30s 后自动过期
- 秒杀场景允许短暂不一致（最终一致即可），不追求强一致

**面试话术**："多实例 Caffeine 一致性：通过 Redis Pub/Sub 广播失效通知，各实例订阅后清除本地缓存；同时 Caffeine 设置短 TTL 兜底，即使消息丢失也能在 30s 内自动过期，保证最终一致。"

---

## 4.8 RabbitMQ 消息可靠性保障（三板斧）

> 延时队列如果消息丢了，超时订单永远不会被取消，库存永久占用。必须保证消息可靠投递：

**1. 生产者可靠性：Confirm + Return 机制**
```java
// RabbitMQConfig 中开启确认
@Bean
public RabbitTemplate rabbitTemplate(ConnectionFactory cf) {
    RabbitTemplate template = new RabbitTemplate(cf);
    template.setConfirmCallback((correlationData, ack, cause) -> {
        if (!ack) {
            log.error("消息发送失败: {}", cause);
        }
    });
    template.setReturnsCallback(returned -> {
        log.warn("消息路由失败: {}", returned.getMessage());
    });
    return template;
}
```

**2. 消息持久化：durable queue + persistent message**
```java
// RabbitMQConfig 中声明持久化队列
@Bean
public Queue orderDelayQueue() {
    return QueueBuilder.durable("order.delay.queue")  // durable=true
        .withArgument("x-message-ttl", 30 * 60 * 1000)  // 30min
        .withArgument("x-dead-letter-exchange", "order.cancel.exchange")
        .build();
}
```

**3. 消费者可靠性：手动 ACK + 幂等消费**
```java
// application.yml
spring:
  rabbitmq:
    listener:
      simple:
        acknowledge-mode: manual  // 手动确认

// OrderCancelConsumer
@RabbitListener(queues = "order.cancel.queue")
public void onMessage(Message message, Channel channel) throws IOException {
    try {
        String orderNo = parseOrderNo(message);
        // 幂等判断：查订单状态，已取消则直接 ACK
        Orders order = orderService.getByOrderNo(orderNo);
        if (order.getStatus() != 0) {
            channel.basicAck(message.getMessageProperties().getDeliveryTag(), false);
            return;
        }
        // 执行取消逻辑：回滚 Redis + MySQL 库存
        orderService.cancelOrder(orderNo);
        channel.basicAck(message.getMessageProperties().getDeliveryTag(), false);
    } catch (Exception e) {
        channel.basicNack(message.getMessageProperties().getDeliveryTag(), false, false);
    }
}
```

**面试话术**："RabbitMQ 消息可靠性三板斧：生产者 Confirm 确认 + 消息持久化 + 消费者手动 ACK；消费幂等靠唯一订单号判断，已处理的订单直接 ACK，避免重复取消。"

---

## 4.9 安全模块（接口鉴权 + 密码加密）

> 秒杀系统必须防止未授权访问和恶意刷接口。

**1. 密码 BCrypt 加密存储**
```java
// UserService 注册逻辑
public void register(String username, String rawPassword) {
    String encoded = new BCryptPasswordEncoder().encode(rawPassword);
    userMapper.insert(new User(username, encoded));
}

// 登录逻辑
public User login(String username, String rawPassword) {
    User user = userMapper.selectByUsername(username);
    if (user == null || !new BCryptPasswordEncoder().matches(rawPassword, user.getPassword())) {
        throw new BusinessException("用户名或密码错误");
    }
    // 登录成功，写入 Session（Spring Session 自动存 Redis）
    session.setAttribute("user", user);
    return user;
}
```

**2. 登录拦截器（校验 Session）**
```java
// AuthInterceptor.java
public class AuthInterceptor implements HandlerInterceptor {
    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
        User user = (User) request.getSession().getAttribute("user");
        if (user == null) {
            response.setStatus(401);
            response.getWriter().write("{\"code\":401,\"message\":\"未登录\"}");
            return false;
        }
        return true;
    }
}

// WebMvcConfig.java 注册拦截器
@Configuration
public class WebMvcConfig implements WebMvcConfigurer {
    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(new AuthInterceptor())
            .addPathPatterns("/api/**")
            .excludePathPatterns("/api/user/login", "/api/user/register");
    }
}
```

**3. 防刷策略补充**
- 限流维度：按 `ip` + `userId` 双维度限流，防止单用户多账号刷接口
- 秒杀接口：同一用户同一商品只能抢一次（Redis Set 记录已抢用户）
```java
// SeckillService 中
String userKey = "seckill:user:" + productId + ":" + userId;
Boolean added = redisTemplate.opsForValue().setIfAbsent(userKey, "1", 1, TimeUnit.HOURS);
if (!added) {
    return Result.seckillFail("你已经抢过了，请勿重复操作");
}
```

---

## 5. MySQL 硬核亮点（简历加分核心）

### 5.1 双层防超卖（Redis 锁 + MySQL 乐观锁）
**第一层** Redisson 分布式锁做并发串行化；**最终兜底**用数据库乐观锁，即使 Redis 锁失效 / 网络抖动也不超卖：
```java
// SeckillService（在锁内执行）
int rows = productMapper.decreaseStockWithVersion(productId); // 返回受影响行数
if (rows == 0) {
    // 乐观锁失败 = 被别人抢先扣完或版本冲突 → 回滚 Redis 预扣，返回售罄
    redisTemplate.opsForValue().increment("seckill:stock:" + productId);
    return Result.seckillFail("手慢了，已抢光");
}
```
### 📁 `.../mapper/ProductMapper.java`
```java
@Update("UPDATE product SET stock = stock - 1, version = version + 1 " +
        "WHERE id = #{id} AND stock > 0 AND version = #{version}")
int decreaseStockWithVersion(@Param("id") Long id, @Param("version") Integer version);
```
> **面试话术**："Redis 分布式锁 + MySQL 乐观锁**双层防超卖**——Redis 锁挡住绝大部分并发，数据库 `version` 字段做最终一致性兜底，保证哪怕锁失效也不会超卖。"

### 5.2 事务与幂等
```java
@Transactional(rollbackFor = Exception.class)   // 异常自动回滚
public Result<Void> createOrder(Long userId, Long productId, String orderNo) {
    // orderNo 由雪花/UUID 生成，配合 uk_order_no 唯一索引 → 重复点击直接唯一键冲突被拦截
    ordersMapper.insert(new Orders(orderNo, userId, productId, 0));
    // ... 扣库存（乐观锁）
}
```
- **幂等三道防线**：前端防重复提交 → `orderNo` 唯一索引 → 消费端去重。
- **消息重复投递**：RabbitMQ 消费幂等同样靠 `uk_order_no`，重复消费抛冲突即忽略。

### 5.3 索引优化 & 慢查询演示
见 §2 表设计与索引注释；慢查询模块把 `EXPLAIN` 前后对比写进 README。

---

## 6. Spring / JUC / MQ 配套亮点

### 6.1 AOP 统一封装
- `@RateLimit` 限流（§4.4）
- `LogAspect`：`@Around` 记录接口耗时、入参出参，定位慢接口
- `GlobalExceptionHandler`：异常统一捕获返回 `Result`（§3）

### 6.2 本地 JUC 二级限流
`Semaphore` 信号量在单机内先限流（§4.4 已用），减少 Redis 压力——**本地 + 分布式双重削峰**。

### 6.3 自定义线程池异步
### 📁 `.../config/ThreadPoolConfig.java`
```java
@Bean("bizExecutor")
public Executor bizExecutor() {
    return new ThreadPoolExecutor(
        4, 8, 60, TimeUnit.SECONDS,
        new LinkedBlockingQueue<>(1000),
        new ThreadFactory() {            // ★必须自定义线程名，方便排查
            private final AtomicInteger i = new AtomicInteger();
            public Thread newThread(Runnable r) { return new Thread(r, "biz-" + i.incrementAndGet()); }
        },
        new ThreadPoolExecutor.CallerRunsPolicy()); // 拒绝策略：调用方执行，不丢任务
}
```
答题记录落库、排行榜更新、订单日志都用 `bizExecutor` 异步执行，**不阻塞秒杀主线程**。

### 6.4 RabbitMQ 延时队列（订单超时自动取消）★ 比 Redis List 更亮眼
**方案 B（推荐）**：死信队列 + TTL 实现 30 分钟未支付自动释放库存。
```java
// RabbitMQConfig：order.delay.exchange -> order.delay.queue(TTL=30min, 死信转发) -> order.cancel.exchange -> order.cancel.queue
// OrderDelayProducer：下单成功后发消息（带 orderNo + productId）
// OrderCancelConsumer：收到消息若订单仍 status=0 → 释放 Redis 库存 + MySQL 乐观锁回补 + 改订单 status=2
```
> **亮点话术**："秒杀下单 30 分钟未支付，RabbitMQ 延时队列自动触发取消，回滚 Redis 缓存和 MySQL 库存，解决死单长期占用库存问题。"——这是纯 Redis List 做不到的"可靠延时 + 至少一次投递"。

### 6.5 三级缓存架构（Caffeine → Redis → MySQL）★ 面试超级高频
```java
// SeckillService.getStock 读路径
Integer stock = caffeine.getIfPresent(key);          // 1. 本地缓存（纳秒级）
if (stock == null) {
    stock = redisTemplate.opsForValue().get(key);    // 2. 分布式缓存
    if (stock == null) {
        stock = productMapper.selectStockById(id);   // 3. 数据库（并回写 Redis + Caffeine）
        redisTemplate.opsForValue().set(key, stock, ttl);
    }
    caffeine.put(key, stock);
}
```
> **面试话术**："三级缓存：Caffeine 抗本地热点（无网络开销）、Redis 抗集群并发、MySQL 做最终数据源。各级设置不同 TTL + 随机抖动防雪崩，更新时先删 Redis 再删 Caffeine 保证一致性。"

### 6.6 其他加分点
- **Jackson 序列化**：`RedisConfig` 统一 JSON（复用 redis-demo），替代 JDK 默认二进制。
- **Knife4j 接口文档**：`@Api` / `@ApiOperation` 注解，一键调试限流、秒杀、排行榜接口。
- **熔断降级**：Redis 节点宕机时走 DB + 本地缓存（`Resilience4j` 或简单 try-catch fallback）。

---

## 7. 最终技术栈（简历直接复制）

**核心技术（主打）**
> Spring Boot 3.x、Redis、Redisson、布隆过滤器（Guava）、滑动窗口限流（AOP + Lua 脚本）、ZSet 积分排行榜、Redisson 分布式锁（可重入 + 看门狗）、三级缓存架构（Caffeine + Redis + MySQL）、缓存三大问题治理（穿透/击穿/雪崩）、Redis Pub/Sub 多实例缓存一致性

**辅助亮点技术（拔高）**
> MySQL 乐观锁（version 字段）、MySQL 事务与幂等设计（唯一索引 + 声明式事务）、联合索引优化 + 慢查询 EXPLAIN、AOP 切面编程（限流 + 日志埋点）、自定义线程池（ThreadPoolExecutor + CallerRunsPolicy）、RabbitMQ 延时队列（死信 + TTL + 消息可靠性三板斧）、Caffeine 本地缓存、Knife4j 接口文档（OpenAPI3）、Spring Session Redis 分布式会话、BCrypt 密码加密、登录拦截器、接口监控与统计埋点、全局异常处理

---

## 8. 简历项目一句话描述（模板）

> **高并发学习课程秒杀系统**：以 Redis 为核心实现缓存三大问题治理（穿透/击穿/雪崩）、Redisson 分布式锁防超卖、AOP + Lua 滑动窗口限流、ZSet 积分排行榜；搭配 MySQL 乐观锁做库存最终兜底，通过事务 + 唯一索引保证下单幂等性；使用 RabbitMQ 延时队列（消息可靠性三板斧）实现超时订单自动回滚；搭建 Caffeine + Redis + MySQL 三级缓存架构，通过 Redis Pub/Sub 实现多实例缓存一致性；集成 BCrypt 密码加密、登录拦截器、接口监控埋点，完整落地后端高并发流量削峰、数据一致性、安全鉴权与可观测性解决方案。

---

## 8.1 Redis Key 设计规范（分层前缀 + 业务语义）

> 统一 Key 命名规范，避免冲突，方便排查和监控。

| 业务场景 | Key 格式 | 示例 | 说明 |
|---|---|---|---|
| 商品库存缓存 | `seckill:stock:{productId}` | `seckill:stock:1001` | 三级缓存中的 Redis 层 |
| 秒杀分布式锁 | `seckill:lock:{productId}` | `seckill:lock:1001` | Redisson 锁 Key |
| 用户抢购记录 | `seckill:user:{productId}:{userId}` | `seckill:user:1001:100` | 防重复抢购，TTL 1h |
| 积分排行榜 | `rank:score` | `rank:score` | ZSet，score 为积分 |
| IP 限流 | `ratelimit:ip:{ip}:{接口}` | `ratelimit:ip:192.168.1.1:seckill` | ZSet 滑动窗口 |
| 答题异步队列 | `answer:queue` | `answer:queue` | Redis List |
| 缓存失效广播 | `cache:invalidate` | `cache:invalidate` | Redis Pub/Sub Channel |
| 分布式 Session | `spring:session:sessions:{sessionId}` | `spring:session:sessions:abc123` | Spring Session 自动管理 |

**命名原则**：
1. **分层前缀**：`业务模块:子模块:具体标识`，如 `seckill:stock:1001`
2. **避免大 Key**：单个 Key 存储数据控制在 KB 级别，布隆过滤器分片
3. **设置 TTL**：除热点商品库存外，其他 Key 必须设置过期时间 + 随机抖动
4. **禁止无意义命名**：如 `key1`、`test`，必须见名知义

---

## 9. 开发顺序（8 阶段，与 AGENTS.md 严格对齐）

> 原则：**骨架 → 数据层 → Redis 主体 → AOP 限流 → 核心秒杀 → MQ 延时 → 安全+配套 → 收尾**。每阶段结束 `git commit` + 写入 `DEVELOP_LOG.md`。

### 阶段 1：项目基础骨架搭建（约 1 天）
- [ ] 完整 `pom.xml`，导入全部规定依赖（Knife4j 使用 `knife4j-openapi3-jakarta-spring-boot-starter 4.4.0`）
- [ ] 按规范创建包分层：`config` / `common` / `aop` / `interceptor` / `entity` / `mapper` / `service` / `controller`
- [ ] 公共基础组件：`Result` 统一返回、`ResultCode` 状态码枚举、`BusinessException` 自定义异常、`GlobalExceptionHandler` 全局异常处理
- [ ] 完整 `application.yml`（Redis、RabbitMQ、MyBatis、线程池、Knife4j 配置）
- [ ] `Knife4jConfig` 接口文档配置
- [ ] ✅ 交付校验：项目无编译错误，启动正常，访问 `http://localhost:8080/doc.html` 可打开接口文档

### 阶段 2：MySQL 数据层设计与持久化（约 1-2 天）
- [ ] `sql/schema.sql`：4 张业务表 `user`、`product`、`orders`、`answer_record`
- [ ] 强制字段/索引：`product.version` 乐观锁、`orders.order_no` 唯一索引、业务查询联合索引
- [ ] 所有 Entity、Mapper 接口、XML 映射文件，实现乐观锁扣库存 SQL、基础 CRUD、分页
- [ ] `MybatisPlusConfig`：分页插件 + 乐观锁拦截器
- [ ] ✅ 交付校验：执行 SQL 正常建表，所有 CRUD 接口可通过 Knife4j 调试

### 阶段 3：Redis 核心模块（项目核心，约 3-4 天）
- [ ] `RedisConfig`：Jackson 序列化，替换默认 JDK 序列化
- [ ] `RedissonConfig`：分布式锁客户端 Bean
- [ ] `CaffeineConfig`：本地缓存配置 + 三级缓存链路（Caffeine→Redis→MySQL）
- [ ] `BloomFilterService`：项目启动预热商品 ID，拦截缓存穿透
- [ ] 缓存防护：热点商品永不过期、TTL 随机抖动、互斥锁防击穿
- [ ] Redisson 可重入锁 + 看门狗自动续期，finally 块强制释放
- [ ] `SessionConfig`：Spring Session Redis 多实例会话共享
- [ ] ZSet 积分排行榜：答题加分、TOP 榜单查询
- [ ] Redis List 简易异步队列 + 自定义线程池批量落库
- [ ] ✅ 交付校验：缓存三大问题、分布式锁、排行榜、异步队列均可单独测试验证

### 阶段 4：AOP 限流与 JUC 本地削峰（约 1-2 天）
- [ ] 自定义 `@RateLimit` 注解
- [ ] `RateLimitAspect` 切面：Lua 脚本实现滑动窗口 IP 限流
- [ ] JUC `Semaphore` 单机本地限流，实现「本地 + 分布式」双重削峰
- [ ] `LogAspect` 接口日志切面：自动记录入参、出参、接口耗时
- [ ] ✅ 交付校验：高频重复请求返回 429 限流提示

### 阶段 5：核心秒杀并发业务（双层防超卖核心，约 2-3 天）
- [ ] `SeckillService` 完整链路：布隆过滤器 → JUC 本地限流 → Redis 分布式限流 → 三级缓存读库存 → Redisson 分布式锁 → Redis 预扣 → MySQL 乐观锁兜底
- [ ] 乐观锁更新失败时，回滚 Redis 预扣库存
- [ ] `@Transactional` 声明式事务，`order_no` 唯一索引实现下单幂等
- [ ] 用户防重复抢购（Redis Set 记录已抢用户）
- [ ] ✅ 交付校验：并发压测无超卖，重复下单被唯一索引拦截

### 阶段 6：RabbitMQ 延时订单模块（约 2 天）
- [ ] `RabbitMQConfig`：死信交换机 + TTL 延时队列，30 分钟未支付自动取消
- [ ] `OrderDelayProducer`：下单发送延时消息（Confirm 确认机制）
- [ ] `OrderCancelConsumer`：消费超时消息，手动 ACK + 幂等判断，回滚 MySQL + Redis 库存
- [ ] ✅ 交付校验：超时订单自动释放库存

### 阶段 7：安全模块 + 配套学习业务（约 2 天）
- [ ] `UserService`：注册登录（BCrypt 密码加密）
- [ ] `AuthInterceptor` 登录拦截器 + `WebMvcConfig` 注册
- [ ] 答题记录提交接口（异步写入 Redis List）
- [ ] 用户答题积分更新排行榜接口
- [ ] 课程商品查询、我的订单分页接口
- [ ] ✅ 交付校验：未登录访问返回 401，登录后可正常调用业务接口

### 阶段 8：项目收尾与完整交付物（约 1-2 天）
- [ ] 编写完整 `README.md`：Mermaid 架构图 + 技术栈 + 全功能清单 + 启动步骤 + 踩坑总结 + 面试问答库
- [ ] 所有接口补充 OpenAPI 注解，Knife4j 完整调试可用
- [ ] 压测验证：JMeter 演示"1000 并发不超卖"，截图放 README（见 §12 压测基线）
- [ ] 汇总全阶段开发内容，写入 `DEVELOP_LOG.md`
- [ ] 统一 Git 提交规范：`feat` 新增 / `fix` 修复 / `refactor` 重构
- [ ] `git push` 上 GitHub，作为你第二个（非 AI）代表作

> ⚠️ **Docker 暂缓**：等项目跑通后，复用 `redis实战项目.md` 的 `Dockerfile + docker-compose.yml` 把 Redis/MySQL/RabbitMQ/App 一键编排即可，半天搞定。

---

## 10. 踩坑清单（面试被追问时能救命）

1. **布隆过滤器误判**：不存在的一定拦截，**存在的可能误判**（误放行到 DB）。所以它是"防穿透第一道"，DB 查不到仍要返回空，不能当绝对正确。
2. **本地 Guava 布隆过滤器 vs 分布式**：多实例部署时每个 JVM 各一份，需**启动时统一预热**全量商品 ID；更彻底的分布式方案是 Redisson `RBloomFilter`（面试可提）。
3. **热点不过期 ≠ 永远不更新**：库存变了要主动删除/更新缓存，否则读到旧值。更新策略：先更新 DB → 再删 Redis（Cache-Aside）。
4. **乐观锁失败要回滚 Redis 预扣**：DB 乐观锁 `rows=0` 时记得 `INCR` 把 Redis 预扣加回去，否则 Redis 与 DB 永久不一致。
5. **分布式锁必须 finally 释放**：异常也不能留死锁；Redisson 的 `unlock` 也要放 finally。
6. **限流 key 维度**：按 `ip` 或 `user` 维度，别全局一个 key，否则一个用户限流全站。
7. **RabbitMQ 延时队列消息丢失**：生产者 confirm + 消费者手动 ACK，保证"至少一次投递"；消费幂等靠 `uk_order_no`。
8. **三级缓存一致性**：更新商品库存时，先更 DB，再删 Redis、再清 Caffeine；读时允许短暂不一致（最终一致）。
9. **线程池拒绝策略**：用 `CallerRunsPolicy`（调用方执行）避免秒杀记录丢失，比 `AbortPolicy` 稳。
10. **`SELECT *` 坑**：订单分页只查必要字段，减少 IO；大表避免回表。
11. **BCrypt 密码长度**：BCrypt 加密后固定 60 字符，`password` 字段至少 `VARCHAR(128)`，别用 `VARCHAR(32)` 导致截断。
12. **Spring Session 序列化**：User 实体必须实现 `Serializable`，否则存 Redis 时报序列化异常。
13. **Redis 大 key 问题**：布隆过滤器如果预热百万级 ID，单个 key 会很大；应分片或控制商品规模在万级以内。
14. **Semaphore 泄漏**：`tryAcquire` 成功但 `proceed` 抛异常时，必须确保 `finally` 中 `release`；否则信号量逐渐耗尽，后续请求全部被限流。
15. **Knife4j 4.4.0 坐标坑**：必须用 `knife4j-openapi3-jakarta-spring-boot-starter`，不要用老的 `knife4j-spring-boot-starter`（那是 OpenAPI2/Swagger2，和 Spring Boot 3 不兼容）。

---

## 11. 面试连环追问自测（照着准备）

- **Q：为什么用 Redis 分布式锁而不直接用 MySQL 乐观锁？**
  A：乐观锁是"最后兜底"，高并发下大量请求打到 DB 会压垮 MySQL；Redis 锁在缓存层就挡掉绝大部分并发，DB 只处理真正抢到的少量请求。
- **Q：Redis 锁失效了怎么办？**
  A：双层设计，DB 乐观锁 `version` 做最终一致性兜底，锁失效也不会超卖。
- **Q：缓存和数据库一致性怎么保证？**
  A：Cache-Aside（先更 DB 再删缓存）+ 延时双删兜底；允许短暂不一致，最终一致。
- **Q：三级缓存每一层挂了怎么办？**
  A：Caffeine 挂=走 Redis；Redis 挂=熔断降级走 DB+本地缓存；DB 挂=整体不可用（极少）。
- **Q：RabbitMQ 延时队列怎么实现？消息丢了怎么办？**
  A：死信队列 + 消息 TTL；confirm 机制 + 手动 ACK 保证不丢；消费幂等靠唯一订单号。
- **Q：滑动窗口限流为什么用 Lua 脚本？**
  A：ZSet 的 `removeRangeByScore` + `opsForZSet().size()` + `expire` 三步操作必须原子执行，否则并发下计数不准。Lua 脚本在 Redis 单线程模型中天然原子。
- **Q：为什么 Caffeine 不直接用 Spring Cache？**
  A：Spring Cache 抽象层太重，本项目需要精确控制 Caffeine 的 `expireAfterWrite`、`maximumSize`、手动 `invalidate`，直接用 Caffeine API 更灵活。
- **Q：多实例下如何保证布隆过滤器一致？**
  A：每个实例启动时从 DB 查全量商品 ID 各自预热本地 Guava 布隆过滤器；商品新增时通过 Redis Pub/Sub 广播通知所有实例重新预热。更彻底方案：用 Redisson `RBloomFilter` 做分布式布隆过滤器。
- **Q：秒杀接口如何防止脚本/机器人？**
  A：① 登录拦截器保证必须登录；② IP + 用户双维度限流；③ 同一用户同一商品只能抢一次（Redis Set 去重）；④ 可扩展验证码 / 答题验证（本项目用答题积分作为前置门槛）。
- **Q：如果 Redis 预扣成功但 MySQL 事务提交失败怎么办？**
  A：事务回滚后，在 catch 块中执行 `INCR` 回补 Redis 库存；如果回补也失败（Redis 也挂了），则记录补偿日志，定时任务重试恢复一致性。
- **Q：你项目的 QPS 大概多少？压测过吗？**
  A：单机压测（见 §12 压测基线），秒杀接口 QPS 约 800-1200，P99 < 200ms；加了三级缓存后读接口 QPS 可达 3000+。

---

## 12. 压测基线（JMeter 验证标准）

> 用 JMeter 对核心接口压测，结果截图放 README，面试时有数据支撑。

| 接口 | 并发数 | 持续时间 | 预期 QPS | 预期 P99 | 校验指标 |
|---|---|---|---|---|---|
| 秒杀下单 `/api/seckill/{id}` | 1000 | 60s | ≥ 800 | < 200ms | **零超卖**（DB 库存 ≥ 0） |
| 商品查询 `/api/product/{id}` | 2000 | 60s | ≥ 3000 | < 50ms | 三级缓存命中率 > 95% |
| 限流验证 `/api/seckill/{id}` | 5000 | 30s | 被限流后返回 429 | - | 超窗口请求全部返回 429 |
| 排行榜查询 `/api/rank/top10` | 1000 | 60s | ≥ 2000 | < 30ms | 返回正确 Top10 |

**压测步骤**：
1. 初始化商品库存 1000，启动项目
2. JMeter 配置 1000 线程循环 1 次，目标秒杀接口
3. 压测结束后检查：`SELECT stock FROM product WHERE id=1001` ≥ 0，`SELECT COUNT(*) FROM orders WHERE product_id=1001` = 1000 - 剩余库存
4. 截图：JMeter 聚合报告 + DB 库存校验 + `EXPLAIN` 慢查询对比

---

## 13. 监控与可观测性（LogAspect 扩展）

> 生产级系统必须有可观测性，本项目通过 `LogAspect` 扩展实现轻量级监控（不引入重型组件如 Prometheus）。

### 13.1 接口耗时统计
通过 AOP 统一统计每个接口的耗时，超过阈值自动告警：

```java
// LogAspect 中扩展
@Around("execution(* com.sygzcd.seckillmall.controller..*.*(..))")
public Object logExecutionTime(ProceedingJoinPoint pjp) throws Throwable {
    long start = System.currentTimeMillis();
    try {
        return pjp.proceed();
    } finally {
        long cost = System.currentTimeMillis() - start;
        log.info("API|{}|{}ms", pjp.getSignature().toShortString(), cost);
        if (cost > 500) { // 慢接口阈值
            log.warn("SLOW_API|{}|{}ms", pjp.getSignature().toShortString(), cost);
        }
    }
}
```

### 13.2 缓存命中率统计
在三级缓存读路径中用 `AtomicLong` 埋点统计缓存命中率：

```java
// 在 SeckillService 中注入计数器
private final AtomicLong cacheHit = new AtomicLong(0);
private final AtomicLong cacheMiss = new AtomicLong(0);

// 读库存时
if (caffeine.getIfPresent(key) != null) {
    cacheHit.incrementAndGet();
} else if (redisTemplate.opsForValue().get(key) != null) {
    cacheHit.incrementAndGet();
} else {
    cacheMiss.incrementAndGet();
}

// 暴露统计接口
@GetMapping("/api/monitor/cache-hit-rate")
public Result<Map<String, Object>> cacheHitRate() {
    long hit = cacheHit.get(), miss = cacheMiss.get();
    double rate = (hit + miss) == 0 ? 0 : (double) hit / (hit + miss) * 100;
    return Result.success(Map.of("hit", hit, "miss", miss, "rate", String.format("%.2f%%", rate)));
}
```

### 13.3 秒杀业务指标

```java
// 统计秒杀成功/失败/限流次数
private final AtomicLong seckillTotal = new AtomicLong(0);
private final AtomicLong seckillSuccess = new AtomicLong(0);
private final AtomicLong seckillRateLimited = new AtomicLong(0);

// 暴露统计接口
@GetMapping("/api/monitor/seckill-stats")
public Result<Map<String, Long>> seckillStats() {
    return Result.success(Map.of(
        "total", seckillTotal.get(),
        "success", seckillSuccess.get(),
        "rateLimited", seckillRateLimited.get()
    ));
}
```

> **面试话术**："通过 LogAspect 统一埋点统计接口耗时，慢接口自动告警；通过 AtomicLong 统计 Redis 缓存命中率和秒杀成功/失败/限流次数，暴露 `/api/monitor` 接口实时查看系统健康状态。"

---

