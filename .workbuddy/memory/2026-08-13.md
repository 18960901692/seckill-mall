# 2026-08-13 工作记录

## LEARNING_GUIDE.md 对齐用户第二批代码修改（8 个提交）

基于 `git log` 定位本次新提交（自 ea56e68 之后）：6c5b99b, 4149187, fe238cc, 987945d, 991c09f, 18f37d0, ad1ec71, a4f9c42。全部重新读源码后落到文档，**未做远程推送**（遵守 AGENTS.md）。

### 本次改动的文档章节
- **Ch4.5.3/4.5.4**：CaffeineConfig 改为 `Cache<String,Product>`（仅缓存商品详情），补"缓存职责拆分"说明；CacheInvalidateConfig 同步。
- **Ch5.4 AuthInterceptor**：`storedSessionId==null` 由"自动续期"改为"踢下线 session.invalidate + 登录已过期"（commit 6c5b99b）。
- **Ch7.2/7.3/7.4**：架构图拆分为"商品详情三级 / 库存直读 Redis"；getById 改为 `tryLock(50,0)` 看门狗续期 + 超时双重检查；7.4 方案B 同步，【已修复】措辞改为当前实现。
- **Ch7.5**：getStock 改为 Redis 直读（不进 Caffeine）；invalidateCache 改 `stringRedisTemplate.convertAndSend` 且只清 Caffeine 商品 key；新增 7.6 ProductController 查 null 抛 NOT_FOUND（commit 4149187）。
- **Ch8.3**：sendDelayMessage 失败改 push `seckill:delay:retry`；泛型 catch 增 `DuplicateKeyException`→`REPEAT_ORDER`（commit fe238cc）。
- **Ch9.4/9.5**：OrderCancelConsumer 加本地重试 3 次 + Nack 重入队 + 耗尽转 `seckill:dead:retry` 补偿队列；cancelOrder `setSql` 增 `version=version+1`（commit 987945d/991c09f）；新增 9.7 节讲 DelayRetryService / DeadLetterRetryService（commit 18f37d0/991c09f，启动类 @EnableScheduling）。
- **Ch13.1 Key 表**：补 `seckill:delay:retry`/`seckill:dead:retry` 两补偿队列，标注库存不进 Caffeine；Ch14.3 失效路径修正。
- **Ch16 缺陷清单**：第13条"对账任务未实现"改为【已修复·中】+ 残余风险（重试3次失败仅 log 丢弃，无人工处理表）；第11条击穿现状改 tryLock(50,0)；第9条死信单消费者补缓解说明；新增第14条 DuplicateKeyException→REPEAT_ORDER 正面例子。
- **Ch17/18**：一分钟内讲清模板补缓存职责拆分与补偿闭环；自测清单对齐。

### 当前源码关键事实（供后续会话参考）
- 缓存职责：Caffeine 仅 Product 详情；库存 `seckill:stock:{id}` 只 Redis，不进本地缓存。
- 补偿队列：`seckill:delay:retry`（DelayRetryService 30s）、`seckill:dead:retry`（DeadLetterRetryService 60s）。
- 取消订单：物理删除 + `stock+1, version+1` 同步；userKey 事务提交后删，允许重抢。
- 防超卖三层：Redisson 锁 / Redis DECR / MySQL 乐观锁(version)；幂等三层：SETNX / 锁 / uk_user_product。

### 残留未修复（仍是面试主动提的点）
答题异步四不足、Key 命名不统一、死信单消费者（已缓解）、本地布隆不共享、取消物理删除丢审计、补偿队列无人工处理表兜底。
