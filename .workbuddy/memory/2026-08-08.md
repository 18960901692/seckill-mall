# 2026-08-08 工作日志

## 完善 LEARNING_GUIDE.md（对齐用户已修复的代码）
用户先改了项目里若干小问题，本次根据源码把 `D:\CodeBase\seckill-mall\LEARNING_GUIDE.md` 更新到与代码一致，并整理格式。

### 已确认的源码现状（本次读取核实）
- `OrderController`：`getOrder`/`cancelOrder` 加归属校验（`order.userId.equals(userId)` → `FORBIDDEN`），异常统一抛给 `GlobalExceptionHandler`，不再 try-catch。
- `BlackListController`：`/add`、`/remove` 加 `@RequireAdmin`；`AdminInterceptor` 读 `session.isAdmin`，`WebMvcConfig` 注册为第 2 顺位拦截器，仅拦截 `/api/blacklist/**`。
- `UserServiceImpl.login`：仅 `admin` 账号 `session.setAttribute("isAdmin", true)`。
- `RankServiceImpl` / `BlackListServiceImpl`：已用 `StringRedisTemplate` + `incrementScore` 累加（ZSet member 不带引号）。
- `SeckillServiceImpl.seckill`：`decremented` 标志 + 中断补偿 `INCR`；`sendDelayMessage` 已移到事务提交之后（try/catch，失败仅 log「将由对账任务补偿」）。
- `ProductServiceImpl.invalidateCache`：广播改用 `stringRedisTemplate.convertAndSend`（修复引号 Bug）；`getById` 缓存击穿互斥改为 `tryLock()` 失败直接 `return selectById`，不再 sleep+递归。
- `AnswerAsyncServiceImpl`：加分只在 `bizExecutor` 任务内一次（`correct` 才 +10），无重复计分。

### 本次对文档的修改
- Ch7 7.4 方案B：互斥锁代码改为当前 `tryLock()` 立即回源，标注【已修复】。
- Ch9.6：替换为带归属校验/统一异常的 `OrderController`；安全警告改为「归属已修复，分页仅返回 records 仍待优化」。
- Ch10.3：替换为带 `@RequireAdmin` 的 `BlackListController` + 三层鉴权说明；警告改「已修复」。
- Ch14.3：Pub/Sub 引号 Bug 由「隐藏 Bug」改为「已修复 + 现状说明 + 面试话术」。
- Ch14.2 顺序口诀：把「发消息」移出事务括号（事务提交→发延时消息→失效缓存→放锁）。
- Ch16 缺陷清单重写：标记【已修复】1(Pub/Sub)、2(Controller吞异常)、3(黑名单权限)、4(订单归属)、6(消息出事务)、11(sleep递归)；保留【未修复】5(物理删除丢审计)、7(答题四不足)、8(Key命名)、9(死信单消费者)、10(本地布隆)；新增【未修复·中】13 延时消息失败后的对账任务未实现。
- Ch17 Q7/Q20、Ch18 自测清单：措辞改为「这些问题我已修复，剩下列出的未修复项才是改进方向」。

### 文档格式与完整性
- 19 章标题统一为 `#` H1（先前已规范化）；内部分节 `##`/`###`。
- 代码围栏 216（偶数，平衡）；正文 3773 行。
- 残留唯一真实缺口：**对账任务未实现**——`sendDelayMessage` 失败仅打日志，无定时补偿，极端情况订单永久占库存。

> 注：AGENTS.md 规定项目交付后禁止推送到远程仓库（git push 等），本次仅改本地文档，未做任何远程操作。
