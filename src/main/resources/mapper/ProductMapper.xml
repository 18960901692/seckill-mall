<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sygzcd.seckillmall.mapper.ProductMapper">

    <!-- 乐观锁扣减库存 -->
    <update id="decreaseStockWithVersion">
        UPDATE product
        SET stock = stock - 1,
            version = version + 1
        WHERE id = #{id}
          AND stock > 0
          AND version = #{version}
    </update>

</mapper>
